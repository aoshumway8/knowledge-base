# /design-research — Agent Roster

IDs only — no file paths. Paths are resolved at runtime via `../design-knowledge/MANIFEST.md`.

---

## Experts

- usability-testing
- survey
- thinking-exercises
- design-evaluation
- design-process
- questions

## Notes

- There are no testers in this skill. All agents are experts — they produce deliverables and diagnoses, not behavioral observations.
- Not all experts are dispatched on every run. Phase 1 routing in `SKILL.md` selects the minimum set needed for the user's question.
- `design-evaluation` is the default routing anchor for ambiguous method-selection questions — when in doubt, start there.
